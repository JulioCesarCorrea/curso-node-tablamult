# Notas:
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de multiplicar       [number] [required]
  -l, --listar   Decide si mostrar la tabla o no      [boolean] [default: false]
  -h, --limite   Es el limite de la multiplicacion                      [number]
```